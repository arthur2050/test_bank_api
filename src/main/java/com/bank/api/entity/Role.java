package com.bank.api.entity;

/**
 * Перечисление ролей пользователей в системе.
 * <p>
 * Используется для разграничения доступа к эндпоинтам:
 * - USER — обычный пользователь
 * - ADMIN — администратор
 */
public enum Role {
    USER, ADMIN
}